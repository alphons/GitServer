namespace GitServer.Services;

public static class Terms
{
	/// <summary>Bump when the terms change in a way users should accept again; stored with each acceptance.</summary>
	public const string CurrentVersion = "2026-09-20";
}
