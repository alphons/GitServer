@page "/User/Settings"
@model GitServer.User.UserSettingsModel
@{
    ViewData["Title"] = "Account instellingen";
}
<div class="container container-sm">
    <h1>Account instellingen</h1>

    @if (!string.IsNullOrEmpty(Model.Message))
    {
        <div class="alert @(Model.IsError ? "alert-danger" : "alert-success")">@Model.Message</div>
    }

    <section class="settings-section">
        <h2>Profiel</h2>
        <form method="post" asp-page-handler="Profile">
            <div class="form-group">
                <label for="displayName">Weergavenaam</label>
                <input id="displayName" name="DisplayName" type="text" class="form-control"
                       value="@Model.DisplayName" />
            </div>
            <div class="form-group">
                <label for="bio">Bio</label>
                <textarea id="bio" name="Bio" class="form-control" rows="3">@Model.Bio</textarea>
            </div>
            <div class="form-group">
                <label for="avatarUrl">Avatar URL</label>
                <input id="avatarUrl" name="AvatarUrl" type="url" class="form-control"
                       value="@Model.AvatarUrl" />
            </div>
            <button type="submit" class="btn btn-primary">Profiel opslaan</button>
        </form>
    </section>

    <section class="settings-section">
        <h2>@(Model.HasPassword ? "Wachtwoord wijzigen" : "Wachtwoord instellen")</h2>
        @if (!Model.HasPassword)
        {
            <p class="text-muted">Je account heeft nog geen wachtwoord (aangemeld via OAuth). Stel een wachtwoord in om git via HTTPS te gebruiken.</p>
        }
        <form method="post" asp-page-handler="Password">
            @if (Model.HasPassword)
            {
                <div class="form-group">
                    <label for="currentPassword">Huidig wachtwoord</label>
                    <input id="currentPassword" name="CurrentPassword" type="password" class="form-control" />
                </div>
            }
            <div class="form-group">
                <label for="newPassword">Nieuw wachtwoord</label>
                <input id="newPassword" name="NewPassword" type="password" class="form-control" minlength="6" />
            </div>
            <button type="submit" class="btn btn-primary">@(Model.HasPassword ? "Wachtwoord wijzigen" : "Wachtwoord instellen")</button>
        </form>
    </section>
</div>
